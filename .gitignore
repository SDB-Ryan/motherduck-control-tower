# Secrets — never commit a token
.env
**/.env

# Python
**/__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Local working notes / session log — never versioned (may name accounts)
SESSION_LOG.md

# Local assistant config — never versioned (holds machine-local paths)
.claude/
